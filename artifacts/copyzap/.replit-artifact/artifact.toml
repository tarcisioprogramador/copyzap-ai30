kind = "web"
previewPath = "/"
title = "CopyZap AI"
version = "1.0.0"
id = "artifacts/copyzap"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19254

[services.development]
run = "pnpm --filter @workspace/copyzap run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/copyzap", "run", "build" ]
publicDir = "artifacts/copyzap/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19254"
BASE_PATH = "/"
